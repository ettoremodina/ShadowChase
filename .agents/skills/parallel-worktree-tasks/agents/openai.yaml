interface:
  display_name: "Parallel Worktree Tasks"
  short_description: "Run independent repo tasks in isolated worktrees"
  default_prompt: "Use $parallel-worktree-tasks to execute these repository tasks concurrently in isolated Git worktrees."
policy:
  allow_implicit_invocation: false
