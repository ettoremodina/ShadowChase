[project]
name = "code-flow-explorer-runtime"
version = "0.1.0"
description = "Local runtime dependencies for the code-flow-explorer skill"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1.6,<3.3",
    "pygments>=2.19,<3",
]

[tool.uv]
package = false
