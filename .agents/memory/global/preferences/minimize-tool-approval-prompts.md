# Minimize tool approval prompts

Batch related safe operations into as few authorization requests as practical. Prefer workspace-native patching, already-approved deterministic commands, and read-only inspection; do not request approval for optional GUI or browser actions unless the user asks for them or they are necessary to complete the task. This preference never relaxes destructive-action safety or authorizes broader command scope.
